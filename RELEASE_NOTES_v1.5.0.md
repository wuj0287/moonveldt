**wwj v1.5.0 — 打开旧文档自动内嵌本地图片（base64）**

**本版新增（v1.5.0）**
- **打开旧 .md 自动迁移图片**：用 wwj 打开其他编辑器写的 Markdown 时，自动检测全文的本地图片引用（相对路径、绝对路径、Windows 盘符路径均可），逐个读取并把图片以 `data:image/...;base64,...` 方式**填入文档**——从此图片跟文档走，拷到哪里都不会丢
- 智能处理：网络图片（http/https/data:）**保留不转换**；文件缺失、格式不支持、超过 15MB 的图片自动跳过，并在打开提示中说明跳过数量
- 安全设计：转换只在内存中完成并标记「有改动（Ctrl+S 保存）」，**不会自动覆盖你的原文件**——确认后按 Ctrl+S 才写回
- 手动重扫：插入菜单新增「图片引用转 base64（重扫全文）」，随时可对当前文档重新执行
- 配套支持 png / jpg / gif / webp / svg / bmp / ico / avif 格式

**工程改进**
- 新增 `tests/embed/test.js` 单元测试（8 种引用场景：相对/带空格/带标题/带引号/网络/缺失等），已接入 CI 自动执行

**主要功能（完整）**
- 三种模式：编辑双栏 / 即时渲染（单栏所见即所得）/ 阅读，`Ctrl+/` 切换
- Mermaid 流程图/时序图/甘特图、KaTeX 数学公式 实时渲染
- 图片粘贴/拖拽自动 base64 内嵌；打开旧文档自动迁移本地图片引用
- 全界面缩放 `Ctrl+滚轮`（50%–300%，自动记忆）
- 专注模式 F8 / 打字机模式 F9 / 最近打开 / 大纲 / 字数统计
- 导出 PDF / HTML；明暗主题 + 自定义 CSS
- 双击 .md 直接打开、Ctrl+S 真实保存回原文件

**安装方式**：Windows 10/11（x64）。运行 `wwj-Setup-1.5.0.exe`（自动注册 .md 关联），或使用便携版。
